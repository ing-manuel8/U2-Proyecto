<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perfil de Usuario</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
  <style>
    .profile-container {
      max-width: 600px;
      margin: 2rem auto;
    }
    .profile-img {
      width: 150px;
      height: 150px;
      object-fit: cover;
      border-radius: 50%;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>

  <!-- Incluir el Navbar -->
  <%- include('./partials/navbar') %>

  <div class="container profile-container">
    <div class="card">
      <div class="card-body text-center">
        <!-- Imagen de perfil o predeterminada -->
        <img src="<%= currentUser.profileImage ? `/uploads/${currentUser.profileImage}` : '/images/user-default.png' %>" 
             alt="Imagen de Perfil" class="profile-img mb-3">
        <h3><%= currentUser.name %></h3>
        <p class="text-muted"><%= currentUser.email %></p>

        <!-- Información adicional -->
        <hr>
        <div class="row text-left">
          <div class="col-md-6">
            <p><strong>Número de Control:</strong> <%= currentUser.controlNumber %></p>
          </div>
          <div class="col-md-6">
            <p><strong>Fecha de Nacimiento:</strong> <%= new Date(currentUser.birthDate).toISOString().slice(0, 10) %></p>
          </div>
          <div class="col-md-6">
            <p><strong>Sexo:</strong> <%= currentUser.sex === 'M' ? 'Masculino' : currentUser.sex === 'F' ? 'Femenino' : 'Otro' %></p>
          </div>
          <div class="col-md-6">
            <p><strong>Rol:</strong> <%= currentUser.role === 'admin' ? 'Administrador' : 'Usuario' %></p>
          </div>
        </div>

        <!-- Botón para editar perfil -->
      </div>
    </div>
  </div>

  <!-- Bootstrap JS y dependencias -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
